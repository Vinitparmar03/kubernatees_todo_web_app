apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-pvc
spec:
  storageClassName: ""   
  volumeName: mongodb-pv

  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 2Gi