apiVersion: v1
kind: Service
metadata:
  name: todo-backend
  labels:
    app: todo-backend

spec:
  selector:
    app: todo-backend

  ports:
    - name: http
      protocol: TCP
      port: 5000
      targetPort: 5000